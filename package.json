
{
  "name": "openai-graphql-worker",
  "version": "1.0.0",
  "description": "openAi API GraphQL Worker",
  "main": "src/index.js",
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "tail": "npx wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0"
  }
}